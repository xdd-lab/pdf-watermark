# 性能优化指南

本文档介绍 PDF 全盲水印系统的性能特性以及如何针对不同场景进行优化。

## 性能特点

### 嵌入性能

水印嵌入的主要开销来自：
1. PDF 页面渲染（约占 40%）
2. 小波 / DCT 变换（约占 35%）
3. 位流构造与纠错编码（约占 15%）
4. PDF 写入与压缩（约占 10%）

**基准测试数据** (单页 A4, DPI 150, CPU: Intel i7):
- 嵌入时间：~0.8-1.2 秒/页
- 提取时间：~0.5-0.8 秒/页

### 内存占用

- 单页处理峰值内存：~50-80 MB（取决于 DPI）
- 多页批处理：线性增长（无内存泄漏）

## 优化建议

### 1. 调整 DPI

DPI 直接影响渲染分辨率与可嵌入容量：

| DPI | 页面尺寸 (A4) | 嵌入时间 | 鲁棒性 | 推荐场景 |
|-----|---------------|----------|--------|----------|
| 120 | 990×1400      | 快       | 一般   | 快速原型 |
| 150 | 1240×1754     | 中等     | 良好   | 日常使用 |
| 180 | 1488×2105     | 较慢     | 很好   | 高鲁棒需求 |
| 220 | 1815×2568     | 慢       | 极好   | 极端场景 |

**建议**：对普通办公文档，使用 **DPI 150-180** 即可兼顾速度与质量。

### 2. 调整水印强度

水印强度决定了系数修改的幅度：

- 低强度 (8-10)：速度快，鲁棒性一般，适合内部流通
- 中等强度 (12-14)：推荐默认值，平衡性能与鲁棒性
- 高强度 (16-20)：适用于需要抵御严重压缩、打印的场景

**建议**：从 **12.0** 开始测试，根据实际攻击强度调节。

### 3. 减少纠错冗余

Reed-Solomon 编码的符号数越多，纠错能力越强但开销越大：

| ecc_symbols | 最大纠错能力 | 编解码时间 | 位流膨胀 |
|-------------|--------------|------------|----------|
| 16          | ~8 字节      | 快         | +16 字节 |
| 32          | ~16 字节     | 中等       | +32 字节 |
| 64          | ~32 字节     | 较慢       | +64 字节 |

**建议**：大多数场景使用 **32** 即可，极端噪声环境可提升至 **48-64**。

### 4. 限制处理页数

对于多页 PDF，可仅嵌入前 N 页或关键页：

```python
watermarker.embed(
    input_pdf="large.pdf",
    output_pdf="watermarked.pdf",
    watermark_text="SECRET",
    max_pages=5  # 仅处理前 5 页
)
```

### 5. 调整输出质量

`quality` 参数控制 PDF 内 JPEG 图像的压缩率：

- 70-80：文件较小，略有损失
- 80-90：推荐范围
- 90-95：几乎无损，文件较大

**示例**：10 页 A4 文档（DPI 150）
- Quality 75：~2.5 MB
- Quality 85：~4.2 MB
- Quality 95：~7.8 MB

**建议**：使用 **85** 作为默认值，体积敏感场景可降至 **80**。

### 6. 批量处理优化

对目录下大量 PDF 进行批量嵌入时，建议：

- 使用命令行 `batch-embed` 子命令
- 考虑使用多进程加速（当前版本顺序处理，可扩展为 ProcessPoolExecutor）

## 文件体积对比

| 原始 PDF | 嵌入后 (DPI 150, Q85) | 嵌入后 (DPI 180, Q85) | 嵌入后 (DPI 150, Q75) |
|----------|-----------------------|-----------------------|-----------------------|
| 500 KB   | ~2.1 MB               | ~3.5 MB               | ~1.4 MB               |
| 1 MB     | ~2.8 MB               | ~4.2 MB               | ~2.0 MB               |
| 5 MB     | ~6.5 MB               | ~9.8 MB               | ~4.8 MB               |

> 体积增大主要源于 PDF 页面转换为图像后重新编码。

## 性能调优示例

### 高速低鲁棒（内部文档）

```python
config = WatermarkConfig(
    embed_strength=10.0,
    ecc_symbols=16,
    dpi=120,
    quality=75
)
```

### 平衡模式（推荐）

```python
config = WatermarkConfig(
    embed_strength=12.0,
    ecc_symbols=32,
    dpi=150,
    quality=85
)
```

### 高鲁棒模式（抗打印扫描）

```python
config = WatermarkConfig(
    embed_strength=16.0,
    ecc_symbols=48,
    dpi=180,
    quality=90
)
```

## 已知限制

1. **大文件处理**：100+ 页 PDF 会较慢，建议分批或仅嵌入关键页
2. **彩色图像**：当前仅利用 Y 通道，未来可扩展至多通道嵌入
3. **矢量图形**：必须先转换为光栅图像，会损失矢量特性

## 后续优化方向

- [ ] 支持 GPU 加速（通过 CuPy 或 CUDA）
- [ ] 实现多进程批处理
- [ ] 支持可选的空域 + 频域混合嵌入
- [ ] 提供 C++ 扩展以加速 DCT 循环
